<div class="kitten">
  <%= link_to (image_tag kitten.image), kitten_path(kitten), class: "kitten-link" %>
  <% if kitten.categories.length > 0 %>
  <ul>
    <% kitten.categories.order(name: :asc).each do |category| %>
      <li><%= category.name %></li>
    <% end %>
  <% end %>
  </ul>
</div>